// This is a dummy file because Expo Router requires a file for every Tabs.Screen name.
// We intercept its tab press to navigate to the modal instead.
import { View } from 'react-native';
export default function CreatePostTab() {
    return <View />;
}
